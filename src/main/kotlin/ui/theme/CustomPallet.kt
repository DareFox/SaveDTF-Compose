package ui.theme

import androidx.compose.material.MaterialTheme
import androidx.compose.ui.graphics.Color

object CustomPallet {
    val success = Color.Green
    val onSuccess = Color.Black

    val successVariant = Color.Green.copy(0.7f)

    val error = Color.Red
    val onError = Color.Black
}